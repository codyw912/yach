[metadata]
author = "yach"
description = "Two yach run --session invocations against one session; turn two is only satisfiable from session context, and the verifier asserts the final file shows both turns landed."
difficulty = "easy"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 600.0
