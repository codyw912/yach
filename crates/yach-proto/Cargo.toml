[package]
name = "yach-proto"
version = "0.1.0"
edition = "2024"
description = "Core protocol types for yach"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
