[package]
name = "yach-adapter-pi-rpc"
version = "0.1.0"
edition = "2024"
description = "Pi RPC adapter for yach"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
yach-proto = { path = "../yach-proto" }

[lints]
workspace = true
